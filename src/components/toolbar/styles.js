const styles = {
  toolbarWrapper: {
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fit, minmax(0, auto))',
    gap: '1rem',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
  },
};

export default styles;
