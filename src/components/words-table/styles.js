const styles = theme => ({
  wordsList: {
    display: 'grid',
    margin: 0,
    padding: 0,
    borderRadius: theme.main.borderRadius.small,
    overflow: 'hidden',
    rowGap: '1px',
  },
});

export default styles;
