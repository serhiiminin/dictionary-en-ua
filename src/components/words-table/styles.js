const styles = theme => ({
  wordsList: {
    margin: 0,
    padding: 0,
    background: theme.main.colors.text,
    borderRadius: '5px',
    overflow: 'hidden',
    display: 'grid',
    rowGap: '1px',
  },
});

export default styles;
