import { variables } from '../../styles/variables';

const styles = {
  addExample: {
    display: 'grid',
    padding: `${variables.padding.medium} 0`,
    justifyContent: 'end',
    alignItems: 'center',
  }
};

export default styles;
