const validationLines = {
  email: 'Invalid email',
  match: "Values don't match",
  passwordMinLength: 'Password must be at least 8 characters long',
  required: 'Required field',
};
export default validationLines;
