const NS = 'LOADING';

const loadingNames = {
  wordsList: `${NS}/words-list`,
  addWord: `${NS}/add-word`,
  searchWord: `${NS}/search-word`,
  deleteWord: `${NS}/deleteWord`,
};

export default loadingNames;
